# frozen_string_literal: true

RSpec.describe Rails::Image::Post::Solution do
  it "has a version number" do
    expect(Rails::Image::Post::Solution::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
